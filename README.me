# Ptero-lite Runner (Deployra)

1. Push repository ini ke GitHub root.
2. Deploy to Deployra:
   - Start Command: `npm start` (atau `node app.js`)
   - Set environment variable: `API_KEY=supersecret123`
3. After deploy, open:
   - `GET /status?key=supersecret123` to check status
4. Panel (InfinityFree) should point to your Deployra base URL.
